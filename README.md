If you already have Git installed and want to do it from your computer (for future updates too):

Open your Malicious URL Detection project folder in VS Code

Open Terminal (in VS Code or CMD)

Run these commands (replace your username):

git init
git add .
git commit -m "Initial commit - Malicious URL Detection"
git branch -M main
git remote add origin 
https://github.com/sushma-sri-123/Malicious-url-detection.git
git push -u origin main
